/**
 * Component Styles (Button, Spinner)
 */

// LoginButton
export const loginButton = {
  textTransform: 'none',
  fontSize: '1rem',
  fontWeight: 500,
}

// LogoutButton
export const logoutButton = {
  textTransform: 'none',
  fontSize: '0.9rem',
}

// LoadingSpinner
export const spinnerContainer = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  gap: 2,
  padding: 5,
  minHeight: '400px',
}

export const spinnerCircle = {
  size: 60,
}
